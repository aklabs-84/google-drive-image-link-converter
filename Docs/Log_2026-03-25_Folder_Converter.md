# 📜 작업 로그 (Work Log) - 2026-03-25

## 📌 작업명
구글 드라이브 폴더 내 이미지 일괄 변환 및 복사 기능 구현

---

## 🛠️ 구현 내용 (Worker & UI Polish)

### 1. **구성 요소 추가**
- **`components/GasGuide.tsx`** [NEW]
  - 구글 앱스 스크립트(GAS)를 통해 사용자가 직접 서버 기능을 배포할 수 있도록 안내하는 단계별 가이드 및 코드 복사 뷰어 컴포넌트입니다.
- **`components/FolderResult.tsx`** [NEW]
  - 폴더 조회가 완료된 후 내부의 모든 이미지 파일 목록을 한 테이블에 모와 `직접 링크`, `추천 링크`, `HTML`, `Markdown` 형태로 일괄 복사할 수 있는 상단 바와 개별 프리뷰 뷰를 렌더링합니다.

### 2. **비즈니스 로직 확장**
- **`components/Converter.tsx`** [MODIFY]
  - 기존 파일링크 감지 정규식에서 파일(`folders/...`) 및 폴더(`folders/...`) 구조를 감지할 수 있도록 구획 확장 가용성을 높였습니다.
  - 가이드 토글 패널 및 `fetch` API 기반의 구글 앱스 스크립트(GAS) 라우팅 로직 연동을 수행했습니다.
  - 이전에 작성한 `gasUrl`을 저장할 수 있게 `localStorage`를 연동하여 UX를 개선했습니다.

---

## 🐞 발생한 문제 및 해결 방안 (AS & Solver)

| 문제 상황 | 원인 | 해결 방안 |
| :--- | :--- | :--- |
| **API 키 노출 문제** | 정적 웹앱에서 파일 목록을 조회하려면 Google Drive API 키가 프론트엔드단에 노출되거나 사용자에게 가이드해야 함. | API 키 생성 없이 비교적 가볍게 무료로 연동할 수 있는 **Google Apps Script (GAS) 웹앱 프록시** 방식을 기획 및 적용. <br> 사용자에 맞춤 스크립트 복사 유도. |

---

## ✅ 검증 결과 (Verification)

1. **테스트 빌드**
   - 명령어: `npm run build`
   - 결과: 통과 (Type Error 없음)
2. **이펙트 확인**
   - 로컬 상의 구동 및 정적 리액트 렌더링에 필요한 플러그인 로드에 이상 없음을 확인했습니다.

---

> **참고**: 향후 사용성 개선 시, 대량 복사된 클립보드 데이터의 줄바꿈(`\n`) 형식에 대한 사용자 설정(콤마, 탭 분리 등) 확장 옵션을 제안할 수 있습니다.
